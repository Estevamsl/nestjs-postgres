import { Controller } from '@nestjs/common';

// @Controller('escola') //endpoint para receber requisições para que esse controller seja chamado para executar a ação desejada
@Controller('escola')
export class EscolaController {}
